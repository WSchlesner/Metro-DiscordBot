"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.InMemoryCache = void 0;
var InMemoryCache = /** @class */ (function () {
    function InMemoryCache() {
        this.entries = new Map();
    }
    InMemoryCache.prototype.get = function (cacheKey) {
        var entry = this.entries.get(cacheKey);
        if (entry === undefined) {
            return undefined;
        }
        if (entry.notAfter <= new Date().getTime() / 1000) {
            this.entries.delete(cacheKey);
            return undefined;
        }
        return entry.value;
    };
    InMemoryCache.prototype.set = function (cacheKey, value, expiry) {
        var currentTime = new Date().getTime() / 1000;
        var notAfter = currentTime + (expiry || 3600);
        this.entries.set(cacheKey, {
            value: value,
            notAfter: notAfter
        });
    };
    return InMemoryCache;
}());
exports.InMemoryCache = InMemoryCache;
